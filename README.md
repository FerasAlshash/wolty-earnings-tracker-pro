## **wolty-earnings-tracker-pro**

Welcome to your project repository! This application is designed to help you track your earnings.

### **How can I edit this code?**

There are a few ways to work with this project locally:

**1. Use your preferred IDE**

You can clone this repository and work with your own Integrated Development Environment (IDE).

The only requirements are having **Node.js** and **npm** installed. You can [install them with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

Follow these steps to get started:

```sh
# Step 1: Clone the repository using the project's Git URL.
git clone https://github.com/FerasAlshash/wolty-earnings-tracker-pro.git

# Step 2: Navigate to the project directory.
cd wolty-earnings-tracker-pro

# Step 3: Install the necessary dependencies.
npm i

# Step 4: Start the development server with auto-reloading and an instant preview.
npm run dev
```

**2. Edit a file directly in GitHub**

- Navigate to the desired file(s) in this repository.
- Click the **"Edit"** button (pencil icon) at the top right of the file view.
- Make your changes and **commit** them.

**3. Use GitHub Codespaces**

- Navigate to the main page of your repository.
- Click on the **"Code"** button (green button) near the top right.
- Select the **"Codespaces"** tab.
- Click on **"New codespace"** to launch a new Codespace environment in your browser.
- Edit files directly within the Codespace and **commit and push** your changes once you're done.

---

### **What technologies are used for this project?**

This project is built with a modern web development stack:

- **Vite** (Build tool)
- **TypeScript** (Language)
- **React** (JavaScript library for building user interfaces)
- **shadcn-ui** (UI components)
- **Tailwind CSS** (CSS framework)

---

### **How can I deploy this project?**

Since this project is a standard React application built with Vite, you can deploy it to any modern hosting platform that supports static or server-side rendering (if configured).

Common deployment options include:

* **Vercel**
* **Netlify**
* **GitHub Pages**
* **AWS Amplify**
```sh
# Run the build command
npm run build
```

